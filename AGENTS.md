# Enterprise Transportation Management System (TMS) Project Rules

이 파일은 대기업용 운송관리시스템(TMS) 개발 프로젝트의 아키텍처, 기술 스택, 핵심 비즈니스 프로세스 및 개발 지침을 정의합니다. 에이전트(Antigravity)는 코드 생성 및 설계 시 본 가이드를 엄격히 준수해야 합니다.

---

## 1. 프로젝트 개요 & 목적
본 프로젝트의 목적은 대규모 물류 처리가 가능한 **대기업 수준의 운송관리시스템(TMS - Transportation Management System)**을 구축하는 것입니다. 주문 접수부터 배차 계획, 실시간 관제(트래킹), 정산에 이르는 전 과정을 자동화하고 최적화합니다.

---

## 2. 기술 스택 (Tech Stack)
- **Frontend**: `Flutter` (Cross-platform)
- **Backend**: `FastAPI` (Python)
- **Database**: `PostgreSQL 11`
- **지도 API**: `Naver Map API` (상세 운송 트래킹 및 경로 표시)

---

## 3. 핵심 비즈니스 프로세스
개발 대상 프로세스는 다음과 같으며, 단계별 비즈니스 로직과 데이터 정합성을 철저히 설계해야 합니다.

### ① 기준정보 (Master Data)
운송 계획 및 실행의 기반이 되는 마스터 데이터를 관리합니다.
- **물류 거점**: 출발지(물류창고, 센터), 도착지(고객사, 배송처) 정보 및 위경도 좌표 관리
- **운송 자원**: 차량(차종, 톤수, 냉장/냉동 여부), 운전기사 정보 및 매핑
- **거래처/단가**: 화주(고객사) 정보, 운송사 정보, 계약별 운송 단가표(거리별, 지역별, 차종별)

### ② 운송오더 (Transportation Order)
화주로부터 발생한 배송 요청을 관리합니다.
- 오더 접수 (ERP 연동 및 수동 등록)
- 오더 정제 및 검증 (출발지/도착지 주소 정제, 배송 희망 시간, 화물 특성 검증)

### ③ 운송계획 (Planning)
물류 효율을 극대화하기 위한 계획 단계입니다.
- **편성 (Consolidation)**: 동일 방향, 인접 배송처의 오더들을 묶어 하나의 운송 그룹(라우트)으로 편성
- **배정 (Allocation)**: 편성된 라우트에 적합한 운송사 및 차량(톤수/옵션) 배정
- **배차 (Dispatch)**: 최종 기사 배정 및 운송 지시(지정 노선 전달)

### ④ 운송트래킹 (Tracking)
운송 중인 차량의 실시간 상태와 위치를 관제합니다.
- **네이버 지도 연동**: 상세 이동 경로 렌더링, 실시간 GPS 마커 표시
- **상태 관제**: 출발 -> 운송 중 -> 도착 예정 시간(ETA) 계산 -> 도착 완료 및 하역 상태 실시간 모니터링

### ⑤ 운송실적 (Execution & Performance)
운송이 완료된 후의 실제 실적 데이터를 수집합니다.
- 계획 대비 실제 운송 시간, 주행 거리, 대기 시간 기록
- 배송 완료 증빙 (전자 서명, 사진 첨부 등) 및 이상 발생(오배송, 파손) 기록

### ⑥ 정산 프로세스 (Settlement)
수집된 운송실적을 바탕으로 비용을 정산합니다.
- **매출 정산**: 화주에게 청구할 운송료 계산 (기본 단가 + 대기료, 회차료 등 추가 비용)
- **매입 정산**: 운송사/기사에게 지급할 지불 운송료 계산
- **세금계산서 및 마감**: 정산 확정 및 증빙 발행 연동 대기 데이터 생성

---

## 4. 에이전트 개발 지침
1. **코드 및 DB 설계**:
   - DB 스키마 설계 시 PostgreSQL 11 버전을 기준으로 지원하는 기능만 사용해야 합니다. (예: 생성 속도 개선, 파티셔닝 한계 등 고려)
   - FastAPI 작성 시 비동기(async/await) 처리를 기본으로 하여 대용량 트래픽을 효율적으로 수용하도록 설계합니다.
   - Flutter App 개발 시 상태 관리 패턴(Bloc, Provider 등 프로젝트 설정에 따름)을 준수하고 네이버 지도 플러그인 연동을 위한 네이티브 브릿지나 웹뷰 연동 방식을 고려합니다.
2. **보안 및 트랜잭션**:
   - 대기업 환경에 맞는 정교한 권한 관리(RBAC - 화주, 운송사, 기사, 관리자 등)를 적용합니다.
   - 정산 및 배차 단계와 같이 데이터 정합성이 중요한 프로세스는 강력한 DB 트랜잭션 관리를 수행합니다.
